<p id="notice"><%= notice %></p>

<h1>Candidate Types</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Points confirmed</th>
      <th>Points rejected</th>
      <th>Points success</th>
      <th>Points failure</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @candidate_types.each do |candidate_type| %>
      <tr>
        <td><%= candidate_type.name %></td>
        <td><%= candidate_type.points_confirmed %></td>
        <td><%= candidate_type.points_rejected %></td>
        <td><%= candidate_type.points_success %></td>
        <td><%= candidate_type.points_failure %></td>
        <td><%= link_to 'Show', candidate_type %></td>
        <td><%= link_to 'Edit', edit_candidate_type_path(candidate_type) %></td>
        <td><%= link_to 'Destroy', candidate_type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Candidate Type', new_candidate_type_path %>
