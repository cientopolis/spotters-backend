<%= form_for(candidate_type) do |f| %>
  <% if candidate_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(candidate_type.errors.count, "error") %> prohibited this candidate_type from being saved:</h2>

      <ul>
      <% candidate_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :points_confirmed %>
    <%= f.number_field :points_confirmed %>
  </div>

  <div class="field">
    <%= f.label :points_rejected %>
    <%= f.number_field :points_rejected %>
  </div>

  <div class="field">
    <%= f.label :points_success %>
    <%= f.number_field :points_success %>
  </div>

  <div class="field">
    <%= f.label :points_failure %>
    <%= f.number_field :points_failure %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
