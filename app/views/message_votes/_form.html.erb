<%= form_for(message_vote) do |f| %>
  <% if message_vote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(message_vote.errors.count, "error") %> prohibited this message_vote from being saved:</h2>

      <ul>
      <% message_vote.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :positive %>
    <%= f.check_box :positive %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :message_id %>
    <%= f.text_field :message_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
